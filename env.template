# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pritomatic_aqi
DB_USER=postgres
DB_PASSWORD=postgres

# Database Sync (only for development)
# Set to 'true' to auto-sync database schema on server start
# WARNING: This will alter existing tables. Use migrations in production.
SYNC_DB=false

# JWT Configuration (if implementing authentication)
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=7d

